<table class="w-full text-left {{ $class ?? '' }}">
    <thead>
        <tr class="text-gray-400 bg-white z-10 sticky" style="top:2px;">
            {{ $columns ?? '' }}
        </tr>
    </thead>
    <tbody class="text-gray-600 dark:text-gray-100">
        {{ $rows ?? '' }}
    </tbody>
</table>
